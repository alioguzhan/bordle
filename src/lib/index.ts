import {
  analyze,
  getAbsents,
  getCorrects,
  getPresents,
  initWordle,
} from "./game";
import { useDict } from "./useDict";

export { analyze, getPresents, getAbsents, getCorrects, useDict, initWordle };
